# components/

This project is a single-page vanilla JS app, so all UI lives in
`index.html` + `script.js` for simplicity and zero build-step deployment.

This folder is reserved for future componentization (e.g. if you migrate to
React/Vue, or split the chat window, sidebar, and composer into separate
HTML partials loaded via fetch). Kept here to match the requested project
structure.
